name: Build & Publish ENERGY App

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      version:
        description: 'Version (without v prefix, e.g. 0.1.0-beta)'
        required: true
        default: '0.1.0-beta'
      energy_app_ref:
        description: 'energy-app git ref (commit, tag, or branch)'
        required: true
        default: 'main'

env:
  REGISTRY: ghcr.io
  IMAGE_BASE: ghcr.io/synctacles

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        include:
          - arch: amd64
            goarch: amd64
            goarm: ""
            base_image: ghcr.io/home-assistant/amd64-base:3.18
            platform: linux/amd64
          - arch: aarch64
            goarch: arm64
            goarm: ""
            base_image: ghcr.io/home-assistant/aarch64-base:3.18
            platform: linux/arm64
          - arch: armv7
            goarch: arm
            goarm: "7"
            base_image: ghcr.io/home-assistant/armv7-base:3.18
            platform: linux/arm/v7

    steps:
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Checkout ha-apps
        uses: actions/checkout@v4

      - name: Checkout energy-app
        uses: actions/checkout@v4
        with:
          repository: synctacles/energy-app
          ref: ${{ inputs.energy_app_ref || 'main' }}
          path: _src/energy-app
          token: ${{ secrets.REPO_TOKEN }}

      - name: Checkout energy-backend
        uses: actions/checkout@v4
        with:
          repository: synctacles/energy-backend
          path: _src/energy-backend
          token: ${{ secrets.REPO_TOKEN }}

      - name: Determine version
        id: meta
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="${{ inputs.version }}"
          fi
          COMMIT=$(cd _src/energy-app && git rev-parse HEAD)
          SHORT=$(cd _src/energy-app && git rev-parse --short HEAD)
          DATE=$(date -u +"%Y-%m-%dT%H:%M:%SZ")

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "commit=$COMMIT" >> $GITHUB_OUTPUT
          echo "short=$SHORT" >> $GITHUB_OUTPUT
          echo "date=$DATE" >> $GITHUB_OUTPUT

          echo "Building ENERGY v${VERSION} from ${SHORT} for ${{ matrix.arch }}"

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'
          cache-dependency-path: _src/energy-app/go.sum

      - name: Build Go binary
        working-directory: _src/energy-app
        env:
          CGO_ENABLED: "0"
          GOOS: linux
          GOARCH: ${{ matrix.goarch }}
          GOARM: ${{ matrix.goarm }}
        run: |
          go build -trimpath \
            -ldflags="-s -w \
              -X main.version=${{ steps.meta.outputs.version }} \
              -X main.commit=${{ steps.meta.outputs.commit }} \
              -X main.buildDate=${{ steps.meta.outputs.date }}" \
            -o $GITHUB_WORKSPACE/energy/energy-addon \
            ./cmd/energy-addon

          echo "Binary size: $(du -h $GITHUB_WORKSPACE/energy/energy-addon | cut -f1)"

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        working-directory: energy
        run: |
          VERSION="${{ steps.meta.outputs.version }}"
          ARCH="${{ matrix.arch }}"
          IMAGE="${{ env.IMAGE_BASE }}/${ARCH}-energy"

          # Build for target platform (QEMU emulation for arm) and push directly
          docker buildx build \
            --platform ${{ matrix.platform }} \
            --build-arg BUILD_FROM=${{ matrix.base_image }} \
            --build-arg VERSION="${VERSION}" \
            --build-arg GIT_COMMIT="${{ steps.meta.outputs.commit }}" \
            --build-arg BUILD_DATE="${{ steps.meta.outputs.date }}" \
            -f Dockerfile.ci \
            -t "${IMAGE}:${VERSION}" \
            -t "${IMAGE}:latest" \
            --push \
            .

          echo "Pushed: ${IMAGE}:${VERSION}"
          echo "Pushed: ${IMAGE}:latest"

      - name: Make package public
        env:
          GH_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
        run: |
          gh api --method PATCH \
            /orgs/synctacles/packages/container/${{ matrix.arch }}-energy \
            -f visibility=public \
            && echo "Package ${{ matrix.arch }}-energy set to public" \
            || echo "Warning: PACKAGES_TOKEN missing or insufficient — set package public manually"

      - name: Verify image
        run: |
          IMAGE="${{ env.IMAGE_BASE }}/${{ matrix.arch }}-energy"
          docker manifest inspect "${IMAGE}:${{ steps.meta.outputs.version }}" \
            | python3 -c "import sys,json; m=json.load(sys.stdin); print('digest:', m.get('config',{}).get('digest','?')[:20])" \
            || true
